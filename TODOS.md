# TODOs

These are explicitly outside the current V1 implementation unless promoted by a
future plan.

- Add a hosted multi-tenant control plane after local-first usage proves the
  data model and trust workflow.
- Add organization-level policy packs for regulated teams.
- Add a visual graph explorer once CLI/dashboard recall behavior is stable.
- Add native package publishing automation after the first public release.

